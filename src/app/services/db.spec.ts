import {SportTapPerson, SportTapActivity} from './SportTapDb';
import angular = require('angular');

describe('creating Activity', () => {
  it("should be listed under user's activities", () => {
    expect(true).toBe(true);
    angular.forEach([1,2,3], x => {
      console.log(x);
    });

  });
});
